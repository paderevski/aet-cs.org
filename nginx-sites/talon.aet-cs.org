server {
	listen [::]:443 ssl;
	listen 443 ssl;
	server_name talon.aet-cs.org;

	root /home/bitnami/talon/frontend/dist;
	index index.html index.htm index.nginx-debian.html;

	location / {
		try_files $uri $uri/ index.html;
	}

    location /api/ {
        proxy_pass http://127.0.0.1:5174/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

	ssl_certificate /etc/letsencrypt/live/aet-cs.org/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/aet-cs.org/privkey.pem;
	include /etc/letsencrypt/options-ssl-nginx.conf;
	ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
	listen 80;
	listen [::]:80;
	server_name talon.aet-cs.org;
	return 301 https://$host$request_uri;
}
